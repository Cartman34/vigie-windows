# Implemented — etat REEL

Ce qui est **reellement** en place aujourd'hui, par ID de fonctionnalite (defini
dans `../targeting/features.md`). On ne reecrit pas le besoin : on donne l'etat,
l'emplacement du code, le comportement observe et les ecarts.

- Tableau d'avancement : **`status.md`**.
