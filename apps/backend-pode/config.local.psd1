@{
    # Configuration LOCALE de cette machine (ignoree par git).
    # Cree a partir de config.local.sample.psd1 lors du passage a une config generique.
    # Ne surcharge QUE ce qui est propre a cette machine.

    ToolsPath = 'C:\EspaceRestreint\Workspaces\AiTeam\LocalAgentAdmin\tools'
}
